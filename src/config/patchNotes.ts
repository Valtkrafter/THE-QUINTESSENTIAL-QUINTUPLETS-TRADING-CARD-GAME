/**
 * Active Single-Version Patchnote Configuration
 *
 * Direct export of the active release patchnote and version schema.
 * ALWAYS overwritten upon each completed task/release.
 */
export { APP_VERSION, CURRENT_PATCH_NOTE } from './version';
export type { PatchNote } from './version';
